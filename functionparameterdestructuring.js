const { use } = require("react");

// this will be use for React 

const user = {
    name : "Anisul",
    age : 25
}

function display(user){
    console.log(user,name);
    console.log(user.age);
        
}